export function Footer() {
  return (
    <footer className="border-t border-border px-6 md:px-12 py-6 flex flex-col md:flex-row justify-between items-center gap-2 relative z-[1]">
      <span className="font-code text-[0.55rem] text-text-muted tracking-[3px]">
        AKASH VARMA &copy; 2024
      </span>
      <span className="text-[0.7rem] text-text-muted">
        Systems that scale. Code that lasts.
      </span>
    </footer>
  );
}
